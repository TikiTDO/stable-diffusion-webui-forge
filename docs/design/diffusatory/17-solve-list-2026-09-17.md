# 17 — Solve list, 2026-09-17

The operator's defects and wants, logged verbatim-in-substance on 2026-09-17 ~17:52 EDT, to be
solved in one pass later. Each item: what he said (condensed), then a *reading* line to be filled
from source before the pass starts. Nothing here is scheduled; the list is the product. **Owner** lines are
claims: write your name before you start an item, push that line, then begin.

## Prompt editor

1. **Ctrl-arrow nesting compounds.** First Ctrl-arrow on `thing` gives `(thing:1.1)` and
   highlights it; the next gives `((thing:1.1):1.1)` and so on. Expected: the existing weight
   group is recognised and its number adjusted in place.

   Owner: Art — landed `75fd7672` 
   Reading: `web/src/domain/promptAttention.ts:adjustPromptAttention`. The first press wraps the word and returns a selection of the *content* (`selectionStart+1 … +content.length`). The enclosing-block branch runs only when `selectionStart === selectionEnd` (L46), so the second press sees a non-empty selection, skips it, and wraps the selected content again with a fresh `1 ± 0.1` → `((thing:1.1):1.1)`. Fix: when a selection exists, check whether it is exactly the content of an enclosing `WEIGHTED_BLOCK` (or the whole block) and adjust that block's weight in place; only otherwise wrap. Forge's `edit-attention.js` does this by testing the characters around the selection. One unit test per case in `promptAttention.test.ts`.
2. **Extract a term group into a movable token.** Highlight `green hill, ancient tree, vista`
   inside a prompt, press a hotkey/button → it becomes its own little prompt group with an on/off
   state; in the source prompt it becomes a draggable sigil. On = its text is inserted at the
   sigil's position; off = nothing.
   Reading: Nothing like it exists; the composition surface is a plain textarea (`PromptComposition.tsx`, 178 lines) with `promptAttention.ts` as its only structural editor. Fix shape: a `PromptGroup { id, label, text, enabled }` list on the draft and a sigil token `⟦g:id⟧` in the prompt text; `PromptComposition` renders sigils as draggable chips (contenteditable or a token-aware textarea overlay), the realiser expands enabled groups in place before send, and item 20's composition blob persists groups. Extraction = select → hotkey → replace selection with sigil + append group. (relates to `11-dynamic-prompt-realizations.md`; the sigil is a composition token)
3. **Prompt variations box goes away.** Replace with one button controlling variations.
   Variations are a txt2img concept; img2img/inpaint probably do not need them at all. (May
   already be gone in a newer build than the one he was on.)
   Reading: `Composer.tsx:483` still renders the variation control ("Generate variation"); whether the *box* is gone depends on the build he was on — check the deployed bundle's date before touching. Fix shape per his earlier discussion: one button on the txt2img stage (count + open-realisations), none on img2img/inpaint; `domain/dynamic prompt realisations` (`11-dynamic-prompt-realizations.md`) supplies the alternatives.
4. **Regional prompts replace the primary prompt on the left** (see Regions).
    Reading: The composer's primary prompt (`PromptComposition.tsx`) and the regional plan (`RegionComposer.tsx` common/cell/background prompts) are separate surfaces bound to the same draft. With item 17, the left column becomes the plan: background prompt on top, one block per region below, common (global/LoRA) prompt as a collapsed strip — the server already forbids extra-network tags in region prompts (`spatial_conditioning.py:57-61`), which tells the layout where LoRAs belong.

## Status and transport

5. **Websocket / long request for status** (and maybe updates) instead of the client polling
   `/diffusatory/api/v1/status`.

   Owner: Sol — in progress 18:2x, branch `sol/status-push` 
   Reading: `web/src/domain/useServerActivity.ts` loops `client.activity()` against `GET /diffusatory/api/v1/status` (`server/mount.py:222`) with an idle/busy delay pair; the descriptor is built each call from `modules.progress.task_activity_snapshot()` + `shared.state` (`mount.py:150-166`). No push transport exists anywhere in `diffusatory/server` (no WebSocket/SSE). Fix shape: one `GET .../status/stream` SSE (StreamingResponse) emitting the same descriptor on `shared.state` change + a heartbeat, with the poll kept as the fallback when the stream errors.
6. **Static security token in `.env`.** The token is regenerated each launch; it is not securing
   anything beyond "which installation talks to which API". A static token in `.env` is enough.

   Owner: Art — landed `9a1d10a1` 
   Reading: Two tokens, and only one is regenerated. The API bearer token is already static: `diffusatory.sh:120-127` writes `local/runtime/api-token` once (`! -s` guard) and reuses it. The **UI session token** is minted per process at `server/access.py:141` (`secrets.token_urlsafe(32)`) and set as a cookie on the UI document (`access.py:110`), so every restart logs the browser out. Fix shape: read the UI token from the same runtime file (or `DIFFUSATORY_UI_TOKEN`), minted once like the API one; keep the bearer path as is. (`diffusatory/server/access.py`)
7. **Refresh checkpoint does not refresh components**, which forces a restart when a refresh is
   needed.

   Owner: Art — landed `96fc1e79` 
   Reading: `client.refreshCheckpoints()` (`web/src/api/forge/client.ts:204`) POSTs `/sdapi/v1/refresh-checkpoints`, which is `shared.refresh_checkpoints()` only (`modules/api/api.py:782`); it then re-reads `/sdapi/v1/sd-models` + `model-profiles` but never `/sdapi/v1/sd-modules`, and the module list that route serves is only rebuilt by Forge's `refresh_models()` (`modules_forge/main_entry.py:143`: checkpoints **and** VAE/text_encoder dirs → `module_list`), which the Gradio refresh button called and nothing in Diffusatory calls. Fix shape: one Diffusatory route `POST /diffusatory/api/v1/models/refresh` that calls `refresh_models()` + `refresh_vae_list()` + `load_textual_inversion_embeddings(force_reload)` and returns checkpoints, modules, profiles in one payload; client replaces the three fetches. (`forge_residency.py` / `residency.py`, `14-model-residency.md`)

## Dead code

8. **Remove old, unused code paths.** A lot of Gradio remains that is not needed and could be
   trivially replaced. (README: the Gradio UI is not served; Git keeps it.)
   Reading: Boot is already a bare FastAPI (`webui.py:51-55`: `FastAPI` + `create_api` + `progress.setup_progress_api` + `mount_diffusatory`; Gradio's `launch` is not called) but `modules/initialize.py` still `import gradio` (L22) and imports `modules.ui`/`gradio_extensions` (L32), and `gradio==6.27.0` stays in `requirements_versions.txt`. 28 of 133 files in `modules/` import gradio; `diffusatory/server` imports only 7 `modules` names + `backend` + `modules_forge.{shared,utils}`. Pass: (1) cut `initialize.py`'s ui imports and the `modules.ui*` reload loop (L107), (2) delete `modules/ui*.py`, `ui_extra_networks*`, `gradio_extensions`, `modules_forge/main_entry.py`'s `gr_` handlers (keep `refresh_models`, item 7), `javascript/`, `html/`, `style.css`, `script.js`, `localizations/`, `extensions-builtin/*` that only add Gradio tabs, (3) drop gradio from requirements and verify `venv` boots; `modules/api/api.py` keeps the sdapi routes the client uses (`sd-models`, `sd-modules`, `options`, `png-info`, `txt2img`, `img2img`, `refresh-*`, `controlnet`). Referrer proof per deletion (`grep -rn` across `modules backend modules_forge diffusatory scripts extensions-builtin`). (inventory `modules/`, `modules_forge/`, `extensions-builtin/` referrers from
   `diffusatory/server/`; delete what nothing imports)

## Edit mode (img2img / inpaint)

9. **Big edit mode and small edit mode are inconsistent.** Small does not show generating images;
   large forces you to watch generating images and cannot switch off. Should be one set of
   controls with two views, not two widgets.

   Owner: Art — in progress 19:03 EDT
    Reading: Two components, two truths. Small = `App.tsx:1237` workspace pane (`stage--editor` + `Stage.tsx`, whose live preview follows `generation.phase` for any kind); large = `App.tsx:1301` `FocusedEditWorkspace.tsx` (712 lines, a `role=dialog`) with its own `showingLiveEdit = generating && kind === "img2img"` (L158) and its own preview block (L234). `editorPresentation` (`App.tsx:268`) is only set by `replaceEditor(...)` callers (L795) — drop/import forces `"focused"` — so the mode is chosen by how the image arrived, not by the user. Fix shape: one `EditorControls` + one `EditorPreview` component with a `layout: "panel" | "dialog"` prop; `FocusedEditWorkspace` shrinks to the dialog frame; live-preview visibility becomes one user toggle shared by both layouts. (`09-img2img-editor-implementation.md`)
10. **Easy switch between big and small edit mode.**

   Owner: Art — in progress 19:03 EDT (with 9)
    Reading: Same root as 9: `editorPresentation` has no user control. Add the toggle in the shared `EditorControls` header (and `Esc`/a shortcut in `features/editor/shortcuts.ts`), persisted per session.
11. **Session variations: collapse, add more, move items between them.** Primary variations for
    the story, sub-variations for one image.
    Reading: State is one flat `EditorVariation[]` in `App.tsx:262` (`domain/editorVariations.ts`: `original | working | variation | inpaint`, one image each) rendered by a 56-line tray; there is no session object, no nesting, no removed bucket. Fix shape: a `Session { id, label, parent?, items: EditorVariationGroup[] , collapsed }` model with `groups` as the unit (item 12), `removed` as a reserved session, drag/move between sessions in the tray; `App.tsx` keeps `activeSessionId` + `activeGroupId` instead of `activeEditorVariationId`.
12. **Inpaint stays on the image being inpainted.** Working edition = inpaint/paint on the original;
    drawing on "Inpaint 1" stays on Inpaint 1. Generate shows the new variation(s) in the big
    preview and in Session variations; multiple candidates from one generate are ONE item there.
    Removing from Session variations moves it to a special "removed" session (own button) for
    restore.
    Reading: Today `appendGeneratedEditorVariations` (`editorVariations.ts:55`) appends one tray item per candidate image, and the working snapshot (`App.tsx:670`) is taken from `editor.initImage`+`editor.mask` on generate — so the mask lives on the working item and the result becomes the active item, which moves the canvas off the image being inpainted. Fix shape: (a) the mask/paint layer is keyed by the *source* variation id and stays there across generates; (b) one generate → one group `{ sourceId, candidates: image[] }` shown as one tray item with a candidate strip and in the big preview; (c) `removeEditorVariation` (`App.tsx:864`) moves the group to the `removed` session instead of splicing it out.

## LoRAs and embeddings

13. **LoRA search is laggy with a large DB.** Virtualised list (embeddings too); smarter fuzzy
    search index.

    Owner: Art — landed (pre-indexed fields, debounce + useDeferredValue, searchLoraCatalog)
    Reading: `components/PromptTools.tsx:120-158` recomputes on every keystroke: for each LoRA, `domain/loras.ts:loraSearchMatch` calls `searchFields(lora)` (rebuilds the field list) and `normalizedField` (per-character `\p{L}\p{N}` regex over every field, plus the query) — O(catalog × fields × length) regex work per keystroke, synchronously in render, then sorts; results are capped at 24/group so render cost is small — the lag is the matching, not the DOM. Fix shape: normalise fields once per catalog load (memo keyed on `catalog.loras`), build a lowercase concatenated index per LoRA, debounce the query ~80 ms, run the match in `useDeferredValue`/a worker for large catalogs; virtualise the grid only if the caps are lifted. Embeddings: same path once they get a picker. (`lora_catalog.py`, `15-lora-prompt-workbench.md`)
14. **Pinned LoRAs have no image, description, or way to get more info.**

    Owner: Art — landed (rich cards with thumbnail, base model badge, description, notes, inspect button)
    Reading: The pinned row (`PromptTools.tsx:256-300`, `active-lora` article) renders name, On/Off, weight and keywords from the *active* record and only looks up the catalog entry (`catalogById`) for keywords — never `preview_url`, `description`, `notes`, base model, which the catalog item already carries (`server/lora_catalog.py:26-45`, from the sidecar/civitai JSON). Fix shape: the pinned card gets the same thumbnail as the library card plus an expand affordance that opens item 15's large view; no new data needed.
15. **"Large LoRA view" (edit LoRAs) was never implemented.**

    Owner: Art — landed (LoraDetailModal edit view over PUT defaults and PUT preview image upload)
    Reading: No large view exists in `web/src`; `PUT /diffusatory/api/v1/loras/{id}/defaults` (`mount.py:264`) already persists per-LoRA defaults (weight/keywords), and the catalog item carries description/notes/preview/base-model, so the edit surface is a client component over an existing route: fields for default weight, trigger keywords, notes, plus preview swap (needs one new `PUT …/preview` route writing beside the safetensors).

## Regions and composition

16. **"Choose a region" overflows the right bar.**
    Reading: No component named "Choose a region" exists; the only "Choose a…" strings are ControlNet issue copy (`features/controlnet/model.ts:91-94`). The overflowing control is `features/regions/RegionComposer.tsx:77` — `region-cell-prompts` lays cell textareas in a CSS grid of `min(columns, 2)` tracks with `minmax(0,1fr)`, inside the right bar; overflow is the bar's width vs the stage stats `<dl>` (L109-112) and the 2-column textarea grid. Confirm in the browser before fixing; likely fix is one column in the bar and letting the stage carry the per-cell prompts (item 17 makes the point moot).
17. **Multiple movable regions, no grid.** Add a region, move and rotate it, give it a prompt.
    A background prompt uses the early-steps composition lock: the first few steps are
    "location + camera angle", then the regions come in.
    Reading: The server already takes what the operator asks for: `SpatialCell.polygon` is four free points (`spatial_conditioning.py:53`), masks are rasterised per polygon (L167-204), and the plan has a `SpatialBackground` (L64). The **grid** is only client-side — `RegionMap.tsx` derives cells from `rows/columns` tracks + one shared `transform` (L29-60) — so "add a region, move it, rotate it" is a client model change: `regions: { id, polygon, prompt }[]` each with its own transform, serialised straight into the existing plan. Missing on the server: a per-region **step window** — the modifier (`L297-323`) applies every region at every step; add `start/end` (fraction of `sampler_steps`, L239) per cell/background and gate in `modifier` by `step` (L302). That is the composition lock: background `{0..k}` steps = location + camera, regions from `k`. Design doc 12 §Still open already lists the Flux/Hires gaps to keep in view. (`12-unified-shot-and-regional-stage.md`, `spatial_conditioning.py`)
18. **Right bar copy.** Lots of pointless text; most of it does not explain the confusing controls.
    Reading: Copy lives inline in `RegionComposer.tsx` (e.g. L60 "The transformable grid is open on the stage."), `PromptTools.tsx`, `Composer.tsx`, and `FocusedEditWorkspace.tsx` — no strings module. Pass: strip every sentence that restates the control's label; keep one line per control that says what changes in the image; move remaining explanations into `KeyboardGuide.tsx`-style help. Do it after 17/9 so the copy is written for the controls that survive.

## Projects and files

19. **Projects.** A project is a directory images are copied into on generation. Names are
    `[order-ish-big-number]-[unique-hash].ext`; the big number leaves room to insert between two
    images with a trivial rename or a few, not a thousand moves (a truly wild reorder may still
    move many, and that is fine).

   Owner: Sol — after 5  
   Reading: designed, not built — `02-domain-model.md` defines Project / Candidate / Frame / Asset with *promotion* (link, not copy) and no server or client code references a project (`grep project diffusatory/` → design docs only). Today images land wherever Forge's `outdir_*_samples` + `samples_filename_pattern` say (`modules/images.py:627-685`; the client passes `override_settings` but no outdir). Smallest version that gives him what he asked: a `projects/<slug>/` directory per project, `POST …/projects/{id}/images` that copies a candidate asset in under `[order]-[hash].ext` where `order` is a 12-digit decimal chosen as the midpoint between neighbours (start at 10^11 steps; renumber the local run only when a gap closes), plus `GET …/projects/{id}` listing in order. The design's frame/version model can sit on top later; the filename scheme *is* the frame order for now. Ties to item 20 (the asset carries its composition). (`16-generation-plan.md`; `02-domain-model.md` for where "project" already exists)
20. **Prompt blob is not saved into the image** — or not loaded back when images are done.

    Owner: Art — landed (composition blob saved in extra_generation_params and round-tripped on drop/import)
    Reading: nothing writes the composition. `client.txt2img/img2img` (`client.ts:296,348`) send only the realised `prompt`/`negative_prompt` plus `diffusatory_spatial_plan`; the only Diffusatory key that reaches the infotext is `"Diffusatory spatial plan"` (`server/spatial_conditioning.py:263 via extra_generation_params`). On load, `domain/imageMetadata.ts:importImageMetadata` reads Forge's `parameters` (`Prompt`, `Negative prompt`, `Model`, sampler…) — so a dropped image restores a flat prompt, never the composition (groups, LoRA workbench, regions). Fix shape: send the composition JSON as a request field the server copies into `p.extra_generation_params["Diffusatory composition"]` (base64 or JSON string; infotext survives PNG `parameters` and JPEG EXIF via Forge's own writer), and have `importImageMetadata` prefer that key, falling back to the flat prompt. Same key carries the variation/group state from item 2.

## Operational note (operator, 17:53)

Running the full stack on airia may need the models copied from Aurora's checkout
(`models/` here holds only the placeholder; Aurora's has the checkpoints/LoRAs).
